# Required: OpenRouter API key for AI chat features
OPENROUTER_API_KEY=your-api-key-here

# JWT signing secret (required in production; has a dev default if unset)
# PM_JWT_SECRET=change-me-in-production

# Comma-separated allowed CORS origins (e.g., http://localhost:3000 for dev)
# PM_CORS_ORIGINS=http://localhost:3000

# Override SQLite database path (default: backend/data/app.db)
# PM_DB_PATH=backend/data/app.db
