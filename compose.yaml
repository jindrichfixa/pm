services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pm-mvp
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - pm-data:/app/backend/data
    restart: unless-stopped

volumes:
  pm-data:
